# NegLift

NegLift 是在本地桌面环境中把胶卷负片处理为正像派生文件的产品。本词汇表区分可测量的保真承诺与主观的图像解释，避免把“好看”误称为“准确”。

## 保真模式

**实用转换**：
面向快速获得一致、可用正像的默认工作流，可使用自动估计和创意调整。
_避免使用_：准确模式、档案模式

**保真模式**：
可试用并生成明确标记的未验证派生文件；只有在已记录且已验证的采集配置下，才提供可测量、可复现、可追溯的条件性验证声明。
_避免使用_：绝对准确、一键准确转换、档案模式

**采集保真**：
在已记录采集配置下，底片信息被可测量地保留的程度。
_避免使用_：历史印相复原、视觉偏好

**渲染保真**：
在声明的胶片、采集、色彩配置、处理和输出条件下，正像渲染可重复获得的程度。
_避免使用_：原始印相、绝对色彩

**已验证采集配置**：
针对一个胶片型号的已记录相机翻拍配置，包含原始图像、相机与镜头、固定背光、倍率、采集色彩配置和验收测量。
_避免使用_：通用相机配置、支持的相机

**胶片标识**：
由制造商、型号、感光度和可选备注构成的稳定标识，用来定义已验证采集配置适用的胶片型号。
_避免使用_：自由名称、自动识别的片种

**配置修订版**：
已验证采集配置的不可变版本。背光、相机、镜头、倍率、原始图像解码版本或标定发生变化时，必须创建新的配置修订版。
_避免使用_：编辑后的配置、更新后的预设

**已被取代的配置**：
已有后续配置修订版的历史配置。它保留供旧项目复现，但不应成为新作业的默认选择。
_避免使用_：已删除配置、当前配置

**配置草稿**：
尚未具备建立已验证采集配置所需参考物、测量或确认条件的本地配置；它可继续编辑，但不能生成已验证保真派生文件。
_避免使用_：未验证配置、临时预设

**保真验收证据**：
在一个已验证采集配置下，对色彩与中性重复性、跨帧一致性和逐通道裁切进行记录、阈值比较并给出通过或未通过结论的质量保证数据。
_避免使用_：视觉检查、准确度分数

**简短检查**：
每卷开始前执行的、用于发现采集配置漂移的简化质量检查；它不能取代按配置规定时间或卷数进行的完整复测。
_避免使用_：完整复测、一次性标定

**完整复测**：
按配置规定的时间或卷数上限重新取得完整保真验收证据的过程。
_避免使用_：简短检查、自动续期

**验收阈值集**：
属于一个配置修订版的、经版本化管理的限制值集合，用于评价保真验收证据；它不是全局通用的专业阈值。
_避免使用_：专业级阈值、通用准确度上限

**阈值修订**：
任何验收阈值集的创建或变更；它必须创建新的配置修订版并重新测量和确认。
_避免使用_：放宽阈值、就地修改

**用户确认的验证**：
测量数据由用户提供或确认，NegLift 负责依据适用验收阈值集记录并评价的已验证结果。
_避免使用_：仪器认证、软件认证

**测量证据记录**：
用户确认的验证所保存的数值、单位、测量日期、操作者、测量方法和参考物标识；它可以附带目标照片或仪器报告的路径和校验值。
_避免使用_：仅通过结论、未说明来源的数值

**整卷保真锁定**：
批处理规则：一卷胶片固定使用同一采集配置、胶片配置、色彩转换、反相锚点和验收阈值集；只允许记录逐帧的白平衡和曝光保真调整。
_避免使用_：逐帧自动标定、全局预设

**保真调整**：
保真派生文件中允许的、被明确记录的白平衡或曝光调整；它与反相、色彩转换和几何变换共同构成可复核的渲染条件。
_避免使用_：创意调色、修复

**调整范围**：
某配置修订版为逐帧白平衡和曝光保真调整规定的可接受区间。超出该范围的文件可以导出，但必须标记为未验证并说明原因。
_避免使用_：全局限制、无限调整

**已验证保真派生文件**：
在已验证采集配置下，保真验收证据通过后生成的十六位标记图像文件；其中可嵌入的信息会嵌入文件，完整谱系会保存在配套记录文件中。
_避免使用_：档案母版、准确导出

**未验证保真派生文件**：
在缺少证据或验收未通过时由保真模式生成的文件；它仍然可追溯，但必须明确说明缺失或未通过的验证条件。
_避免使用_：已验证保真派生文件、档案母版

**保真模式试用状态**：
没有已验证采集配置时允许使用的保真模式状态。它始终显示为未验证，并提供建立或选择配置的入口。
_避免使用_：自动验证、隐藏的保真模式

**谱系记录文件**：
与派生文件配套的机器可读记录，包含源文件校验值、采集配置、色彩配置、锚点、参数、处理状态和验证结果。
_避免使用_：预设、会话文件

**原始采集文件**：
由用户在其文件系统中保管的原始图像文件。谱系记录文件以校验值、原路径和可选相对路径与它回链，不复制或接管它。
_避免使用_：内部资料库、已导入原件

**源文件失配**：
记录的原始采集文件找不到，或其校验值与谱系记录文件不一致的状态；它会阻止文件继承已验证状态，但不阻止生成带原因的未验证文件。
_避免使用_：路径变化、无影响的文件移动

**相机原始数字底片文件**：
包含可验证相机原始数据和采集条件的数字底片格式输入；它可在满足其他条件时进入保真模式。
_避免使用_：已渲染数字底片、来源不明数字底片

**视觉质检条件**：
进行人工视觉质量保证时所记录的显示器名称、色彩配置文件标识、系统显示设置、检查日期和操作者确认；它不表示软件完成了显示器硬件校准。
_避免使用_：已校准复选框、软件校准

**修复派生文件**：
使用降噪、除尘、生成式修补或创意调色的派生文件，因此与保真派生文件不同。
_避免使用_：保真派生文件、母版

**父保真派生文件**：
修复派生文件所引用的、未经修复处理的已验证保真派生文件；两者通过校验值与处理差异回链，并可直接比较。
_避免使用_：被覆盖的原文件、无关联版本

**保真模式预览功能**：
在独立、跨配置验证积累完成前发布的保真模式阶段；它只能使用“用户确认的验证”表述，不能宣称软件已独立验证保真。
_避免使用_：已认证保真、专业准确保证

**公开参考测试集**：
可选的真实测试资产或资料来源；仅在再分发许可明确时提交资产到仓库，不包含用户原始图像，测量记录仅用于具体配置的验证声明。
_避免使用_：客户测试集、仅合成测试

**第一阶段完成门槛**：
保真模式预览的发布条件：完整的未验证工作流、状态与命名、十六位标记图像及谱系记录、旧预设迁移、基础自动测试和真实负片 RAW 冒烟测试。完整测量报告仅是将特定配置标记为“用户确认的验证”的条件，不是使用或预览发布门槛（见 ADR-0002）。
_避免使用_：界面完成、全平台覆盖
